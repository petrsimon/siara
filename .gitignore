node_modules/
dist/
*.log

# Operational DB — rebuilt from APIs on cold start
siara.db
siara.db-*

# Env / secrets
.env
.env.*
!.env.example

# Team-specific config (roster, Jira IDs, emails) — operator-supplied, never committed
siara.config.json

# Generated dashboard (published via CI, not committed)
dashboard.html
# Local review artifacts — contain real reviewer names, never commit
data/
siara.distribution.html
public/
.staticrypt.json
siara.db*
siara.*.config.json
*.db
*.db-shm
*.db-wal
